#include <iostream>
using namespace std;

int main()
{
	int a[1000], b[2000] = { 0 };
	int n;
	int i = 0;
	while (cin >> n)
	{
		if (b[n + 1000] == 0)
			a[i++] = n;
		b[n + 1000] = 1;
	}
	for (int k = 0; k < i; k++)
	{
		int temp = k;
		for (int j = k + 1; j < i; j++)
		{
			if (a[j] < a[temp])temp = j;
		}
		if (temp != k)swap(a[k], a[temp]);
		cout << a[k] << " ";
	}
	return 0;
}
