#include <iostream>
using namespace std;

int main()
{
	int n;
	cin >> n;
	int a[50];
	for (int i = 0; i < n; i++)
		cin >> a[i];
	int x;
	cin >> x;
	int k = 0;
	for (int i = 0; i < n; i++)
	{
		if (a[i] != x)
			a[k++] = a[i];
	}
	for (int i = 0; i < k; i++)
		cout << a[i]<<" ";
	return 0;
}
