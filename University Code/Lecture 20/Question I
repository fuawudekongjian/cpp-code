#include <iostream>
#include <algorithm>
#include <set>
#include <vector>

using namespace std;

bool os(int a)
{
    int ans = 0;
    while (a)
    {
        ans += a % 10;
        a /= 10;
    }
    if (ans % 2 == 0)
        return true;
    return false;
}

int main()
{
    int n, m;
    bool flag = false;
    cin >> n >> m;

    set<int> setA;
    int x;

    // 读取第一组数并处理
    while (n--)
    {
        cin >> x;
        if (os(x))
            setA.insert(x);
    }

    vector<int> ans;

    // 读取第二组数并查找相同元素
    while (m--)
    {
        cin >> x;
        if (os(x))
        {
            if (setA.find(x)!= setA.end())
            {
                ans.push_back(x);
                flag = true;
            }
        }
    }

    // 后续输出部分
    if (flag)
    {
        sort(ans.begin(), ans.end());
        for (size_t i = 0; i < ans.size(); i++)
        {
            if (i > 0)
                cout << "<";
            cout << ans[i];
        }
    }
    else
        cout << "none";

    return 0;
}
