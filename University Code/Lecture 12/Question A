#include <iostream>
#include <string>
using namespace std;
int main()
{
	string a;
	cin >> a;
	int n = a.length();
	for (int i = 0; i < n; i++)
	{
		if (a[i] != 'a' && a[i] != 'e' && a[i] != 'o' && a[i] != 'i' && a[i] != 'u'
			&& a[i] != 'A' && a[i] != 'E' && a[i] != 'I' && a[i] != 'O' && a[i] != 'U')
			cout << a[i];
	}
	return 0;
}
