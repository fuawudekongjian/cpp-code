#include <iostream>
#include <string>
using namespace std;
int main()
{
	string a;
	cin >> a;
	int n = a.length();
	for (int i = 0; i < n; i++)
	{
		for (int k = 0; k < n - 1 - i; k++)
			cout << " ";
		for (int j = 0; j <= i; j++)
			cout << a[j];
		cout << endl;
	}
	return 0;
}
