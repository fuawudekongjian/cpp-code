#include <iostream>
using namespace std;

int f(int n)
{
	int x = n/10;
	if (x == 0)
		return n;
	else
	{
		int y = n%10;
		return y+f(x);
	}
}
int main()
{
	cout << "B" << endl;
	cout << "A" << endl;
	cout << "C" << endl;
}
