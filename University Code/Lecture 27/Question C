#include <iostream>
#include <string>
using namespace std;
int main()
{
	int n;
	cin >> n;
	string a;
	cin >> a;
	int len = a.length();
	for (int i = 0; i < len; i++)
		a[i] = (a[i] - 'a' + n) % 26 + 'a';
	cout << a;
	return 0;
}
