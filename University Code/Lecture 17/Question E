#include <iostream>
using namespace std;

int main()
{
	int n;
	cin >> n;
	int a[100];
	int ans = 1;
	for (int i = 0; i < n; i++)
		cin >> a[i];
	int temp;
	cin >> temp;
	for (int i = 0; i < n; i++)
		if (a[i] > temp)ans++;
	cout << ans;
	return 0;
}
