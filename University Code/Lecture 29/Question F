#include <iostream>
#include <cstring>
using namespace std;

int main()
{
	char a[100];
	cin >> a;
	int n = strlen(a);
	for (int i = 0; i < n; i++)
	{
		if (a[i] == a[i + 1])
		{
			cout << a[i + 1];
			i++;
			continue;
		}
		else cout << a[i];
	}
	return 0;
}
