#include <iostream>
using namespace std;
int main()
{
	int n, a[100]={0}, m = 0;
	cin >> n;
	while (n > 0)
	{
		a[m++] = n % 2;
		n /= 2;
	}
	for (int i = m-1; i >= 0; i--)
		cout << a[i];
	return 0;
}
