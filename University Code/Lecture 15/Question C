#include <iostream>
#include<iomanip>
using namespace std;
int main()
{
	int a[3][5];
	for (int i = 0; i < 3; i++)
	{
		for (int j = 0; j < 5; j++)
		{
			cin >> a[i][j];
			if (a[i][j] != 0)
			{
				cout << setw(3) << i+1 << setw(3) << j+1 << setw(3) << a[i][j] << endl;
			}
		}
	}
}
