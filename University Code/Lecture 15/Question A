#include <iostream>
using namespace std;
int main()
{
	int n;
	cin >> n;
	for (int i = 1; i <= n; i++)
	{
		for (int k = 1; k < i; k++)
			cout << "00" << " ";
		int a = i * 10 + i;
		for (int j = 0; j < n-i+1; j++)
		{
			cout << a << " ";
			a++;
		}
		cout << endl;
	}
	return 0;
}
