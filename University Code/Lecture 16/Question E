#include <iostream>
using namespace std;
int main()
{
	int a, b;
	cin >> a >> b;
	int x[101][101];
	for (int i = 0; i < a; i++)
	{
		for (int k = 0; k < b; k++)
			cin >> x[i][k];
	}
	int w=0;
	for (int i = 0; i < a; i++)
	{
		int r = 0, c = 0;
		for (int k = 0; k < b; k++)
		{
			if (x[i][k] > x[i][c])c = k;
		}
		for (int j = 0; j < a; j++)
		{
			if (x[j][c] < x[r][c])r = j;
		}
		if (i == r)w=1;
	}
	if (w)cout << "Yes";
	else cout << "No";
	return 0;
}
