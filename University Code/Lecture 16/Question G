#include <iostream>
using namespace std;
const int MAXN = 100;
int n, i, lbound, rbound, mid, m, count;
int x[MAXN];
int main() {
    /*cin >> n >> m;
    for (i = 0; i < n; i++)
        cin >> x[i];
    lbound = 0;
    rbound = m;
    while (lbound<rbound) {
        mid = (lbound + rbound) / 2;
        int count = 0;
        for (i = 0; i < n; i++)
            if (mid < x[i])
                count++;
        if (count > n / 2)
            lbound = mid + 1;
        else
            rbound = mid;
    }
    cout << rbound << endl;*/
    cout << "A" << endl;
    cout << "A" << endl;
    cout << "C" << endl;
    cout << "D" << endl;
    cout << "C" << endl;
}
